{
  "name": "@wechat-clone/server-gateway",
  "version": "0.1.0",
  "private": true,
  "description": "API and WebSocket gateway",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --build",
    "lint": "eslint src/"
  },
  "dependencies": {
    "@wechat-clone/shared": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
