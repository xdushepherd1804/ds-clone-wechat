{
  "name": "@wechat-clone/server-search",
  "version": "0.1.0",
  "private": true,
  "description": "Search service — full-text search across messages, contacts, and groups",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --build",
    "lint": "eslint src/"
  },
  "dependencies": {
    "@prisma/client": "^6.0.0",
    "@wechat-clone/shared": "workspace:*",
    "mongodb": "^6.12.0"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
