---
id: T004
name: 用户认证服务
status: failed
phase: P1
priority: high
dependencies: [T002, T003]
agent: backend-1
estimated_hours: 8
---

## 目标

实现完整的用户注册、登录、Token 认证、Session 管理、用户信息管理服务。

## 功能需求

### API 接口

```
POST   /api/auth/register     - 注册
POST   /api/auth/login         - 登录
POST   /api/auth/logout        - 登出
POST   /api/auth/refresh       - 刷新 Token
GET    /api/users/me           - 获取当前用户信息
PUT    /api/users/me           - 更新个人资料
GET    /api/users/:id          - 获取用户信息
POST   /api/users/search       - 搜索用户
PUT    /api/users/me/avatar    - 上传头像
PUT    /api/users/me/password  - 修改密码
```

### 安全设计

- 密码: bcrypt 哈希，最少 8 位
- JWT: access_token (15min) + refresh_token (7d)
- Session: Redis 存储，支持多设备
- 限流: 登录接口 5次/分钟/IP
- 防刷: 注册需验证码 (开发阶段可跳过)

### 数据流

```
注册: Client → Gateway → Auth Service → PostgreSQL (users 表)
登录: Client → Gateway → Auth Service → 验证密码 → 签发 JWT → 写入 Redis Session
鉴权: Client → API Gateway → JWT Middleware → 解析 Token → 注入上下文
```

## 交付物

- [ ] 完整的 Auth Service 代码 (NestJS/Go)
- [ ] JWT 签发与验证中间件
- [ ] Redis Session 管理
- [ ] 参数校验 (class-validator / go-playground)
- [ ] 单元测试 (注册/登录/Token刷新)
- [ ] API 文档 (Swagger/OpenAPI)

## 测试标准

1. 注册新用户成功，重复注册失败
2. 正确密码登录成功，错误密码登录失败
3. Token 过期后可刷新
4. 未认证请求被网关拦截 (401)
5. 并发登录不受影响
